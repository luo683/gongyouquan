# Dependencies
node_modules/
.pnpm-store/

# Builds and coverage
dist/
build/
out/
coverage/
.next/
.vite/
*.tsbuildinfo

# Runtime and local data
.env
.env.*
!.env.example
*.log
logs/
.tmp/
tmp/

# Databases, uploads and local volumes
*.sqlite
*.sqlite3
*.db
postgres-data/
pgdata/
uploads/
backups/

# Secrets and local credentials
*.pem
*.key
*.p12
*.pfx
secrets/
# The backup container mounts this as /etc/age. Only recipient.pub is supposed to
# live here, but ignoring the whole directory is the only rule that survives
# someone dropping the private half in it "temporarily, just to test the decrypt".
infra/deploy/age/

# OS and editor files
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# Electron artifacts
release/
appout/

# Test artifacts
playwright-report/
test-results/

# Local agent and tool state
.evox/
.claude/
.codex/
